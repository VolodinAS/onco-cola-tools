## `ProgressMessage` — простой генератор строки прогресса

Класс `ProgressMessage` помогает отслеживать ход выполнения длительных операций (например, обработка файлов, импорт данных и т.п.) и выводить удобочитаемую строку с ключевыми метриками:

- **Прогресс**: сколько из скольки обработано (`processed/total`)
- **Процент завершения**
- **Скорость обработки** (Items Per Second — IPS)
- **Прошедшее время** (Elapsed Time — ET)
- **Оставшееся время** (Remaining Time — RT)

Формат вывода:
```
 || 123/1000//877 || 12.30% || IPS:  4.567 || ET: 00ч00ч27с || RT: 00ч03ч12с
```

> ⚠️ Если скорость = 0 (ещё не обработано ни одного элемента), оставшееся время отображается как `∞`.

---

### 🔧 Как использовать

1. **Инициализация** перед началом операции:

```python
from progress_message import ProgressMessage

pm = ProgressMessage(total=1000)  # всего 1000 элементов
```

2. **Обновление прогресса** по мере обработки:

```python
for i in range(1, 1001):
    # ... делаем полезную работу ...
    pm.update(i)
    print(pm.msg)  # логируем через log(pm.msg)
    print(f"Обработка [N/M]{pm.msg}")  # или можно добавить в строку
```

3. **Гибкая инициализация** (если вы уже начали обработку):

```python
pm = ProgressMessage(total=500, processed=100, start_time=your_start_timestamp)
```

---

### ✅ Преимущества

- Безопасен к делению на ноль
- Защищён от некорректных входных данных (`total <= 0` → исключение)
- Поддерживает граничные случаи (начало работы, нулевая скорость)
- Человекочитаемый формат времени
- Легко интегрируется в логгеры, CLI, Telegram-боты и т.д.